---
const { platform, username, tld } = Astro.props;
---
<a href={`http://www.${platform}.${tld}/${username}`}>{platform}</a>

<style>
    a {
        padding: 0.5rem 1rem;
        color: white;
        background-color: #bc9541;
        text-decoration: none;
    }
</style>